<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="250dp"
    android:layout_height="140dp">
    <ImageView
        android:id="@+id/video_cover"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />
<TextView android:id="@+id/video_read_num"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_alignParentBottom="true"
    android:textColor="@color/white"
    android:text="10712"
    android:drawableLeft="@mipmap/icon_read"
    android:drawablePadding="6dp"
    android:layout_marginLeft="12dp"
    android:layout_marginBottom="12dp"
    android:layout_marginTop="6dp"
    />
    <TextView android:id="@+id/video_title"
        style="@style/fontWhite_16"
        android:layout_above="@id/video_read_num"
        android:layout_alignLeft="@id/video_read_num"
        android:text="百灵鸟"/>

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_alignParentBottom="true"
        android:layout_margin="18dp"
        android:src="@mipmap/video_icon_play_small"/>
</RelativeLayout>